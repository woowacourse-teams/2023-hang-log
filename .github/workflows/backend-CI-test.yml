name: Java CI with Gradle

on:
  pull_request:
    branches: [ main, develop ]

defaults:
  run:
    working-directory: backend

permissions:
  contents: read

jobs:
  build:
    if: contains(github.event.pull_request.labels.*.name, 'BE') 
    runs-on: ubuntu-22.04
    permissions:
      pull-requests: write

    steps:
    - name: Repository Checkout
      uses: actions/checkout@v3
      
    - name: test
      run : |
          echo "Secrets: ${{secrets.KAKAO_CLIENT_ID}}"
    
#     - name: Access Secrets
#       uses: microsoft/variable-substitution@v1
#       with:
#         files: ./backend/src/main/resources/application.yml 
#       env:
#         GOOGLE_CLIENT_ID: ${{ secrets.GOOGLE_CLIENT_ID }}
#         GOOGLE_CLIENT_SECRET: ${ {secrets.GOOGLE_CLIENT_SECRET} } 
#         GOOGLE_REDIRECT_URI: ${ {secrets.GOOGLE_REDIRECT_URI} }
#         GOOGLE_TOKEN_URI: ${ {secrets.GOOGLE_TOKEN_URI} }
#         GOOGLE_USER_INFO: ${ {secrets.GOOGLE_USER_INFO} }
        
#         KAKAO_CLIENT_ID: ${ {secrets.KAKAO_CLIENT_ID} }
#         KAKAO_CLINET_SECRET: ${ {secrets.KAKAO_CLIENT_SECRET} }
#         KAKAO_REDIRECT_URI: ${ {secrets.KAKAO_REDIRECT_URI} }
#         KAKAO_TOKEN_URI: ${ {secrets.KAKAO_TOKEN_URI} }
#         KAKAO_USER_INFO: ${ {secrets.KAKAO_USER_INFO} }
    - name: test2
      env: 
          APPLICAION_YML: ${{ secrets.APPLICATION_YML }}
      run: echo $APPLICATION_YML | base64 --decode > ./src/main/resources/application-dev.yml
    


    - name: Set up JDK 17
      uses: actions/setup-java@v3
      with:
        java-version: '17'
        distribution: 'corretto'
        
#     - name: cache gradle
#       uses: actions/cache@v2
#       with:
#           path: |
#             ~/.gradle/caches
#             ~/.gradle/wrapper
#           key: ${{ runner.os }}-gradle-${{ hashFiles('**/*.gradle*', '**/gradle-wrapper.properties') }}
#           restore-keys: |
#             ${{ runner.os }}-gradle-
    
#     - name: Build with Custom YAML
#       run: application-dev.yml ./src/main/resources/application-dev.yml
    
    - name: chmod gradlew
      run: chmod +x gradlew
        
    - name: Build with Gradle
      run: ./gradlew build
      
    - name: Run Coverage
      run: ./gradlew test
      
    - name: run jacocoTestCoverage
      id: jacoco
      uses: madrapps/jacoco-report@v1.5
      with:
        title: 📝 Jacoco Test Coverage
        paths: ${{ github.workspace }}/**/build/reports/jacoco/test/jacocoTestReport.xml
        token: ${{ secrets.GITHUB_TOKEN }}
        min-coverage-overall: 85
        min-coverage-changed-files: 85
        update-comment: true
